<form method="post" action="{{ path('app_vagas_delete', {'id': vaga.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ vaga.id) }}">
    <button class="btn">Delete</button>
</form>
