<form method="post" action="{{ path('app_planos_delete', {'id': plano.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ plano.id) }}">
    <button class="btn">Delete</button>
</form>
