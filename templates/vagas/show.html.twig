{% extends 'base.html.twig' %}

{% block title %}Vagas{% endblock %}

{% block body %}
    <h1>Vagas</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ vaga.id }}</td>
            </tr>
            <tr>
                <th>Tipo</th>
                <td>{{ vaga.tipo }}</td>
            </tr>
            <tr>
                <th>Ocupada</th>
                <td>{{ vaga.ocupada ? 'Yes' : 'No' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_vagas_index') }}">back to list</a>

    <a href="{{ path('app_vagas_edit', {'id': vaga.id}) }}">edit</a>

    {{ include('vagas/_delete_form.html.twig') }}
{% endblock %}
