<form method="post" action="{{ path('app_veiculo_delete', {'id': veiculo.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ veiculo.id) }}">
    <button class="btn">Delete</button>
</form>
